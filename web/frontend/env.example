# XWall Web 前端 - 示例环境变量
#
# 使用说明：
# 1. 将本文件复制为以下任意一个（按需选择）：
#    - .env.local          # 本地开发（已在 .gitignore 忽略）
#    - .env.development    # 开发环境默认值
#    - .env.production     # 生产构建时使用
#
# 备注：
# - 不要在 URL 末尾添加斜杠
# - 开发环境如未设置 VITE_API_BASE，将使用 '/api'，Vite 会默认代理到 http://localhost:8083（见 vite.config.js）
# - 如果在此处设置绝对 URL，将绕过代理，请求会直接发送到该后端

# 后端 API 基础地址（不要以斜杠结尾）
# 示例：
# - 开发（通过代理，推荐）：留空
# - 开发（直连后端）：http://localhost:8083
# - 生产（同源部署，由后端同时托管前端）：留空
# - 生产（前后端分离）：https://api.example.com
VITE_API_BASE=

version: '3.8'

services:
  oqqwall:
    build: .
    container_name: oqqwall
    restart: unless-stopped
    ports:
      - "8082:8082"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./logs:/app/data/logs
    environment:
      - LLM_API_KEY=${LLM_API_KEY}
      - DEBUG=${DEBUG:-false}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    networks:
      - oqqwall_net
    # no external queue service required by default
    
  # 可选：PostgreSQL数据库
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: oqqwall_db
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_DB: oqqwall
  #     POSTGRES_USER: oqqwall
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-oqqwall123}
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - oqqwall_net

networks:
  oqqwall_net:
    driver: bridge

volumes:
  # postgres_data:
